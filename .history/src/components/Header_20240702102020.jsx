import PropTypes from "prop-types"

function Header({text}) {
  return (
    <header>
        <div className="container">
            <h2></h2>
        </div>
    </header>
  )
}

export default Header
